persistence:
  enabled: false

proxy:
  enabled: true
  proto: https
  hostname: archiva.test

ingress:
  enabled: true
  tls:
    - hosts:
        - test.example.com
      secretName: archiva-test-tls-secret
